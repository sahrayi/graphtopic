# GraphTopic

GraphTopic is being rewritten to match the latest architecture described in the
project's paper.

The legacy `0.0.2` implementation was removed before starting the rewrite. It
remains available in the Git history and under the `legacy-v0.0.2` tag.

## Status

The new implementation has not been published yet.

## License

MIT License

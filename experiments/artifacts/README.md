# Local experiment artifacts

Place downloaded or migrated datasets, embeddings, neighbor caches, sparse graphs,
and assignments here. Everything except this file and `.gitignore` is ignored.
Never commit corpus text, multi-GB embeddings, generated caches, or model downloads.
